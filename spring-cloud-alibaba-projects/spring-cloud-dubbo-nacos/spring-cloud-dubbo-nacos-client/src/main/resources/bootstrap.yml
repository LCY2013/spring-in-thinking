dubbo:
  cloud:
    subscribed-services: spring-cloud-dubbo-nacos-server
  protocols:
    dubbo:
      port: -1

spring:
  application:
    name: spring-cloud-dubbo-nacos-client
  main:
    allow-bean-definition-overriding: true
  cloud:
    nacos:
      username: nacos
      password: nacos
      discovery:
        server-addr: 127.0.0.1:8848

