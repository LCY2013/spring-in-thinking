Spring IOC 配置元信息
    一、Bean自定义配置
        1、基于XML
        2、基于Properties
        3、基于Java Annotation
        4、基于Java API
    二、IOC容器配置
        1、基于XML
        2、基于Java Annotation
        3、基于Java API
    三、外部化配置
        1、基于Java注解

Spring应用上下文
    ApplicationContext除了提供IOC容器的功能还有：
        1、面向切面(AOP)
        2、配置元信息(Configuration metadata)
        3、资源管理(Resources)
        4、事件(Event)
        5、国际化(i18n)
        6、注解(Annotations)
        7、Environments抽象(Environment Abstraction)