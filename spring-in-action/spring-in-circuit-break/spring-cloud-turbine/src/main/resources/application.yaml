server:
  port: 9000

management:
  endpoint:
    health:
      show-details: always

spring:
  cloud:
    consul:
      host: localhost
      port: 8500
      discovery:
        prefer-ip-address: true

turbine:
  aggregator:
    cluster-config: customer-service
  app-config: customer-service